Clause order:

1) SELECT
2) FROM
3) WHERE
4) GROUP BY
5) HAVING
6) ORDER BY

HAVING is the "clean" way to filter a query that has been aggregated (i.e. uses a GROUP BY), but this is also commonly done using a subquery, which you will learn about in a later lesson.
